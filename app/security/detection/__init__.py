"""Layer 2 — Detection Layer package.

Contains the six deterministic scanner adapters and the shared subprocess +
output-parsing helpers they build on. The parallel runner stage
(``DetectionStage``) lives in :mod:`app.security.detection.runner` (task 4.1).
"""
